# Deviations — Unresolved

Genuinely open: needs a user decision or a manual read of conflicting source material before any
task should build further on the affected area.

| # | Task | Change | Reason | Class | Status |
|---|---|---|---|---|---|
| A25 | S1-D05-T1b | Incidence attribution: `borne_by[payer] = assessed·(1 − π·(1 − enf_CA))` for wage/necessaries instruments, remainder to the employing producers' owner classes; profit tax shifted by return differential; rent instruments unshifted | No functional form is specified for this; a literal counterfactual re-run of the wage formula (tax fully inside `w_nat`) puts the whole burden on the employer at π = 0 — the opposite of the intended incidence story and of Doc 05's acceptance (equality at Combination Act = 1). **Flagged for manual review**: the incidence formula and the tax-shifting prose it's meant to implement disagree with each other, and someone needs to read both and decide which one is wrong before this is closed. | Necessary | needs manual review of the formula vs. the prose it implements |
| A46 | S1-D05-T7 | `three_bands.yaml` never reaches a manufactory within 2,000 years on any of seeds 1-5, so Doc 05's scenario acceptance is only partially met (termination, no NaN, and value conservation all hold — see `TestUntilGameOverScenario`) | Two compounding causes, both beyond a bug fix: (1) `core/trees.PRODUCTION_CHAIN`/`meta/trees._walk_chain` gate every node on its *predecessor* being lit, so a nation that never herds (e.g. `nation_valley`, ~40-100k population, FIELD+WORKSHOP+MINE, no HERDING) can never light `HERDING_WITH_DEPENDENTS` and is permanently stuck at node 1 of 11, regardless of population or actual economic activity — fixing this means redesigning the chain to allow alternate/branching paths, which needs the design document's actual stage-branching intent (the user asked me not to consult it, and not to redesign this without more direction). (2) A herding nation (`nation_steppe`, fixed by A45 to reach `BOUND_LABOUR`) has its own field stuck at a fraction of one land share after 2,000 years — needs an economic tuning pass, explicitly Doc 08's job ("balance run... tuning pass on params/default.yaml if needed"), not Doc 05's. All 5 seeds show near-identical Tree II progression despite different seeds, which may itself be worth a look when this is picked up again. | Uncertain (user informed, not blocking) | awaiting either a design-document reading + chain redesign, or Doc 08's tuning pass |
