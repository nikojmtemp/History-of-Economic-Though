# Deviations — In Progress

Classification is decided; the correction or follow-up task has not landed yet. Check this file
before starting related tasks (S1-D08-T3, S1-D05-T1b follow-ups, CI) — it may change what you build.

| # | Task | Change | Reason | Class | Status |
|---|---|---|---|---|---|
| A26 | 03/08 | Law churn: enforcement ≈ `J_cust·A_S/(A_S+opp)` ≈ 0.03 < `enf_min` 0.1 in the STATE nation, so laws lapse every `k_lapse` years and are re-enacted, each lapse an `O −1` | Genuine equilibrium of the enforcement formula with Doc 03's `enf_min`/`k_lapse` placeholders — a tuning item, not a code defect. Measured this session (S3-D08, `tools/sweep.py`) that varying `k_lapse` alone (3 vs 8) barely moves `nation_valley`'s regression rate (40.0 vs 41.7 per 100 years on `three_bands_ai.yaml`, seeds 1-2, 60 years) — see A59, a very likely bigger contributor to the same symptom. | Necessary → S1-D08-T3 | awaiting Doc 08 tuning pass; A59 measured as a probable bigger factor than `k_lapse` alone |
| A35 | S1-D05-T1b | Tax farming re-derives the farmed instruments each year and repays from total collections; lagged π and the farm loan are stashed under string keys in `nation.incidence`; shifted burden is redistributed nation-wide (class-keyed tables) | `IncidenceTable` has no roster/π fields (`world.py` is out of this task's scope). Follow-up: add `IncidenceTable.assessed_pi` in a later `world.py` edit. | Necessary | follow-up `world.py` edit still pending (not picked up by the D05 world.py field pass in b67d35c) |
| A49 | S2-D06 | Doc 06's balance-run acceptance (20 seeds × `--ai all`, 600 years: manufactory in ≥15 seeds, imposed-then-breached treaty in ≥10, hegemony game-over in ≥5, no nation ending before year 100 in >4) is not run | `tools/batch.py`/`tools/sweep.py` now exist (S3-D08), so the tooling half of this blocker is resolved — a full 600-year × 20-seed run is very likely to still fail on A46 (Tree II's chain blocks manufactory on `three_bands_ai.yaml`, confirmed again this session at 0/3 seeds within 500 years, see A46/A60) and hasn't been re-run pending that. `tests/test_ai.py`'s unit-level acceptance is unaffected and done. | Necessary → A46 | tooling ready; awaiting A46 |
| A56 | S3-D08 | `tools/metrics.herds_tamed_year`/an equivalent Tree I "field settled" detector, both built on `node_lit` events, return `None` for a band's *automatic* taming/settling (confirmed empirically on `scenarios/band_grassland.yaml`: seat reaches STATE, a `HERD_OWNERS` record with `wealth.herd > 0` exists, but no `node_lit` event was ever recorded) | `engine/band.py`'s `tame_herd` sets `location.tree1.nodes[DOMESTICATED_HERDS].lit = True` directly (its own module docstring: "band.py fires taming directly rather than through the legislation pipeline"), bypassing `meta/trees.evaluate_gates`, which is the only place a `node_lit` event is emitted. `tools/test_scenarios.py`'s herds-become-property test works around this by checking `nation.seat`/`A_S`/the `HERD_OWNERS` record directly instead of the node-lit event; `tools/metrics.py`'s docstring for `herds_tamed_year` says so. Not fixed here: `engine/band.py` and `meta/trees.py` belong to Docs 02/05, not 08. | Necessary | logged; a fix belongs to whoever next touches `engine/band.py`'s handover functions |
| A57 | S3-D08 | Doc 08's "retainer dismissal is emergent" scenario test (`tests/test_scenarios.py::test_retainer_dismissal_is_emergent`) is `xfail(strict=True)`, not a real assertion | Measured on `scenarios/three_bands.yaml` (null sovereigns), seeds 1-3, 300 years: RETAINERS only ever appears for `nation_steppe` (peak ≈ 74) and never drops ≥ 50% within 40 years of its peak. The trigger the doc names ("opening a rare workshop or a route") needs the LUXURIES Tree I gate (`has_rare and craftsmen_present` or `route_reaches_luxuries`, `core/trees.py`) to fire, which never happens without a sovereign action — and a null sovereign never takes one. Forcing the gate open directly (rather than via a null sovereign) would test a different, narrower claim than the one written. | Uncertain (user informed, not blocking) | needs either a scenario where a null sovereign's own heuristics can plausibly open a rare workshop/route, or the test's own wording revisited |
| A58 | S3-D08 | Doc 08's "treaty breach, weak vs strong State" scenario test is `xfail(strict=True)` | `politics/interests.DEMANDS` (the self-enactment table) has no tariff law under any Interest — `MERCHANT`'s demands are FREE_TRADE/NAVIGATION_ACT/CHARTERED_COMPANY/PUBLIC_CREDIT/repeal-CUSTOMS only. So there is no path by which a nation's own State strength organically determines whether its tariff law gets enacted (and therefore whether an imposed `TARIFF_CEILING` gets breached) — the only lever is to set `LawState` directly, which tests `trade.treaties.check_breach` (already covered by `tests/test_treaties.py`), not "a weak State breaches, a strong one doesn't." | Uncertain (user informed, not blocking) | needs either a tariff-type demand added to some Interest's table (Doc 03's `politics/interests.py`, not 08's to add unasked) or the test's wording revisited |
| A59 | S3-D08 | Doc 08's "regression to equilibrium" scenario test is `xfail(strict=True)`: measured on `scenarios/three_bands_ai.yaml` seed 1, `nation_valley` regresses in 25 of the 26 years from year 6 to year 31 (one single-year gap; every other year re-triggers) | There is no observed post-resolution window where `U_dis` stays below `U_crit` and Tree II nodes stay lit — the spiral re-triggers almost immediately, essentially every year, rather than settling. This is a measured instance of the same symptom A26 already names (chronic sub-`enf_min` enforcement / churn), on a nation actually reaching STATE, not a new independent root cause — but it's a specific, reproducible number (25/26 years) that A26 didn't have. | Necessary → A70 | re-measured (S3-D07): the spiral window was *never* full (`spiral_years` 0 every year) — the yearly re-trigger was `discrete_triggers` firing on the `revolt` of a ~1-person record (`ratio` 8.0), not enforcement churn. Fixed by A70's crowd floor and cooldown: 0-4 regressions per 300 years on the same scenario. A26's law churn is real but was not this symptom's cause |
| A60 | S3-D08 | Doc 08's "laissez-faire viability" scenario test is `xfail(strict=True)`: a hand-driven policy (ENACT Protection of Property, REPEAL known restraints, queued each year via `core.actions.enqueue` directly — no change to `stock/ai/*`) reaches a manufactory in 0 of 3 seeds within 500 years on `scenarios/three_bands.yaml` | This is A46's already-logged root cause (`nation_valley` never herds, so `core/trees.PRODUCTION_CHAIN`'s sequential gating leaves it permanently stuck at node 1 of 11) confirmed again under a *more* favourable policy than a null sovereign, at a *shorter* horizon (500 vs. A46's 2,000 years) — a stronger, not weaker, confirmation of A46. Also noted in passing: this same policy left running past hegemony game-over reaches it at ≈ year 29 on this scenario (one nation gets a decisive early lead over two other null-sovereign nations almost immediately) — worth knowing before reusing `three_bands.yaml` for a similar test. | Necessary → A46 | same root cause as A46 |
| A61 | S3-D08 | Doc 08's "funding hides the cost of war" scenario test (`tests/test_scenarios.py::test_funding_hides_the_cost_of_war`) is `xfail(strict=True)` with a `NotImplementedError` body — not attempted, not just unresolved | An apples-to-apples comparison needs two wars identical except for `debt_policy`/`funding_mode`, which needs a controlled security setup (matched `M`/doctrine/PSV on both sides). Unlike `trade.treaties.impose` (a one-call fixture `tests/test_treaties.py` already leans on), `security.war` has no equivalent "impose a matched war" entry point — every attempt either needs a multi-hundred-year organic run (where war timing/outcome isn't controllable enough to isolate funding as the one variable) or enough hand-built `WarState`/security scaffolding to risk testing the scaffolding instead of the funding claim. | Uncertain (user informed, not blocking) | needs either a war-fixture entry point (Doc 04's, not 08's to add unasked) or a materially larger test-construction effort than this pass budgeted |
| A62 | S3-D08 | "End of sweep 1" balance-run gate (20 seeds × 1,000 years, null sovereigns, `scenarios/three_bands.yaml`): invariants held (0 non-finite `A_S` across 20 seeds × up to 1,000 years × 3 nations); per-nation emergence over the full 1,000 years (not the hegemony-truncated ~28-year runs `tools/batch.py`'s default early-exit gives — this run disabled that) was `nation_valley` fields 20/20 herds 0/20, `nation_steppe` herds 20/20 fields 15/20, `nation_forest` herds 0/20 fields 0/20 | `nation_forest` starts at `forest_1`, which has neither `grazing` nor `arable` (only `game`+`timber`) — structurally unable to tame a herd or settle a field from its start location, and never migrates in 1,000 years (`engine/band.move`'s trigger is depletion crossing a threshold; `forest_1`'s `game_yield` 5.6 is high enough that depletion apparently never does). Pooled across all 60 nation-seed instances: herds 20/60 (0.33), fields 35/60 (0.58) — fields clears the doc's ≥0.5 bar, herds doesn't, entirely because of one structurally-blocked nation on this map, not a broken mechanic (the two nations that *can* reach each resource reach it in 100%/75-100% of seeds). Whether the ≥0.5 bar is meant per-nation-type pooled or some other aggregation isn't specified; reported as measured rather than interpreted into a pass/fail. | Necessary (measured, reported) | structural block removed by A71 (`forest_2` is a clearing with wild herds; the herding script seeks it): `nation_forest` tames herds by year ~15 in every measured seed. The pooled sweep itself has not been re-run |
| A63 | S3-D08 | "End of sweep 2" (06 acceptance + competitiveness/pacing) and "End of sweep 3" (manual checklist + a logged 1-hour *fast*-speed play session) balance-run gates were not run this session | End of sweep 2 is already logged as blocked on A46 (A49); end of sweep 3 needs a live, wall-clock hour of interactive play against the sweep-3 UI server, which weeks of same-session compute can't substitute for and wasn't attempted. `tools/metrics.py`'s pacing/competitiveness functions exist and are exercised by `tools/sweep.py`, so the instruments for both gates are ready; only the runs themselves are outstanding. | Uncertain (user informed, not blocking) | awaiting A46 (sweep 2) and an actual logged play session (sweep 3) |
| A64 | S3-D07 | `core/world.py`'s `Location` gained optional `x`/`y`/`name` and `Nation` an optional `name` (presentation only; the engine never reads them), and `sim/scenario.py` accepts those keys. `scripts/gen_three_bands.py` and the three 24-location scenarios now carry the authored chart grid, re-drawn as a straight-line planar embedding (coast/steppe as a wheel round `steppe_1`, enclosed by `valley_4 – highland_4 – plains_hub – forest_4`) | Doc 07's map had no crossing-free source: the generator threw its grid away and the spring layout it fell back on gave no planarity guarantee (7 of 40 edges crossed on the grid itself, two straight through nodes). `api/layout.py` now starts from the authored grid and anneals (crossing penalty ≫ every aesthetic term; polish-only when the start is already planar); `tests/test_layout.py` pins every shipped scenario to zero crossings and ≥30px edge-to-node clearance. Doc 01 owns `world.py`/`scenario.py`; the fields are additive with `None` defaults and no test or engine path reads them. | Necessary | done; a new map without authored `x`/`y` still gets the annealer, but only the test guarantees planarity |
| A65 | S3-D07 | `engine/band.py`'s `step_band` skips its depletion-triggered auto-move for a band whose queue already holds a `BAND_MOVE` (contact, taming, settling and barter still run) | Its own docstring says a sovereign's queued `BAND_*` actions "can call the functions above directly instead", but nothing checked the queue, so the player's (or a script's) chosen move could be pre-empted the same year by the heuristic move and then applied from the wrong location. Doc 02 owns the module; the change is one guard. Not changed: a band that settles or tames at step 13 hands over before a queued `BAND_MOVE` applies at step 14, so `validate` rejects it there and the API still emits a `resolved_band_move` feed entry (cost 0) — the resolved event reports the boundary, not the handler's outcome. | Necessary | done; whether a rejected-at-apply action should feed differently is Doc 05/07's call |
| A67 | S3-D07 | `sim/year.apply_actions` marks an action it skips (illegal by step 14, or declined by its handler) on the action's payload (`_rejected`), and resolves `spend: "bar"` on `ENACT`/`REPEAL` to `validate`'s cost at that moment (`_spent` records it). `_apply_treaty_action` honours an `initiator` in an `ACCEPT_TREATY` payload. The API feeds `rejected_*` ("Not applied") instead of `resolved_*` for a skipped action, and reports the amount actually spent | Two truthfulness gaps in Doc 07's feed: (1) `advance_one_year` emitted "done" for every queued action whether or not step 14 applied it — the default player's first move (the valley band settles at step 13 of year 0, so its queued `BAND_MOVE` is illegal at step 14) was reported as "Moved"; (2) `legislation.enact/repeal` pass their bar and deduct A_S by `payload["spend"]`, which no player path ever set, so a player's enactment cost nothing and passed only if support already cleared the bar — while the feed said "Enacted". A quote taken at enqueue is stale by step 14 (step 12 re-apportions authority), hence "bar" = the cost when applied, the same number `validate` checks against A_S there. Doc 05 owns `year.py`; both are small guards and the engine reads neither marker. AI scripts still pass a numeric `spend` and are unaffected. | Necessary | done; `tests/test_api.py` covers the rejected move, the applied move, and that an enacted law is actually in force |
| A69 | S3-D07 | `meta/hegemony.world_shares` counts flags only once `ScoreboardParams.h_min_settled` (2) living nations hold a seat beyond BAND; until then every `flags_n` is 0 and the countdown never starts (or resets, per the existing rule). `scenarios/late_start.yaml` re-sized to 900 / 90 / 90 (was 350 / 340 / 150). `tests/test_finance_meta.py`'s 2,000-year "every seed terminates" acceptance rescoped to 600 years of invariants and value conservation without a termination assertion | Measured: the countdown started in year 2 with the valley at 0.97 capital share against two ~100-person bands — the flags measured "who settled first", not dominance — and the game was over by year 33 on every seed. With the gate, `three_bands_ai.yaml` seeds 2-3 stay competitive to year 300 and seed 1 ends by a war at year 115-126. `late_start` only reached game over ≤ 100 through that band-era window (its header already calls the size gap an approximation); at 900 / 90 / 90 the valley earns its 0.75 after both rivals have tamed (year ~15) and the game ends at year 39 with distinct winners. Null-sovereign worlds (nobody declares war) can now settle into a balance of powers: `three_bands.yaml` seed 1 ends at year 1135, seeds 2-5 hold 0.50/0.50, 0.49/0.51, 0.70/0.30, 0.50/0.50 for 2,000 years — an end needs a sovereign to act, which is the design's point (DD §1.3-1.4). A year floor (`h_earliest_year`) was tried and dropped: the seat gate alone holds the countdown off | Necessary | done; whether `h_min_settled` should be "all living nations" is a Doc 08 call |
| A72 | S3-D07 | Measured, not changed: with the A68-A71 opening, `three_bands_ai.yaml` seeds 1-3 over 300 years produce 1 war (seed 1, year ~115, which ends that game) and 0 treaties, with 1-4 routes | All three nations sit in `dominant_interest == LANDED` from their first settled year, so `ai/scripts.LANDED_SCRIPT`'s `declare_war_on_land_hunger` / `propose_treaty_with_hostile_neighbour` rules *are* selected — their conditions just rarely hold. Diagnosing which condition (land hunger, hostility, strength ratio) never clears is one per-year print of the rule inputs; tuning it is Doc 06/08's pass, not this one's | Necessary (measured, reported) | diagnosed and tuned in A76 |
